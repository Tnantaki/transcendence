from django.apps import AppConfig

class UploadfileConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "uploadfile"
    # from .models import delete_file_relate_to_fileupload_table
    
